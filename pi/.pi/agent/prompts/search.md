---
description: Web search with automatic result condensation
argument-hint: "<search-query>"
---

Search the web for: $ARGUMENTS

After getting the raw results, use the search-processor sub-agent to condense and format them into a clear, structured summary. The search-processor is already configured to use minimax/minimax-m2.5:free model.

Present the condensed summary to the user.
